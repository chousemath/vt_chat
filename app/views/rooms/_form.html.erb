<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :video_url %>
    <%= form.text_field :video_url, id: :room_video_url %>
  </div>

  <div class="field">
    <%= form.label :video_token %>
    <%= form.text_field :video_token, id: :room_video_token %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :room_name %>
  </div>

  <div class="field">
    <%= form.label :room_type %>
    <%= form.text_field :room_type, id: :room_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
